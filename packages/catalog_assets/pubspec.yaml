name: catalog_assets
description: Process-wide global catalog asset cache for catalog and conversation leaves.
version: 0.1.0
publish_to: none

environment:
  sdk: ^3.11.0

dev_dependencies:
  lints: ^6.0.0
  test: ^1.25.0

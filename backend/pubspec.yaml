name: chat_demo_backend
description: Local demo backend for chat scroll view pagination testing.
publish_to: none

environment:
  sdk: ^3.11.0

dependencies:
  shelf: ^1.4.2
  shelf_router: ^1.1.4
  sqlite3: ^2.7.5

dev_dependencies:
  test: ^1.25.14
  http: ^1.4.0
